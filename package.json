{
    "name": "ga4-dashboard",
    "version": "2.0.0",
    "description": "GA4 Multi-Client Analytics Dashboard (Next.js frontend + Supabase backend)",
    "private": true,
    "scripts": {
        "dev": "npm run dev --prefix frontend",
        "build": "npm run build --prefix frontend",
        "start": "npm run start --prefix frontend",
        "install-all": "npm install --prefix frontend",
        "lint": "npm run lint --prefix frontend",
        "db:push": "supabase db push",
        "functions:deploy": "supabase functions deploy daily-sync --no-verify-jwt && supabase functions deploy create-client --no-verify-jwt"
    }
}
